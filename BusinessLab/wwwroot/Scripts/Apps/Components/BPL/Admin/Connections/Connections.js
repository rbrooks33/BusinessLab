Apps.Define([], function () {
    let Me = {
        Initialize: function (callback) {
            callback();
        },
        Model: {
            Connections: []

        }
    };
    return Me;
});